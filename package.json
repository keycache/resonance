{
  "name": "resonance",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && touch dist/.nojekyll",
    "preview": "astro preview",
    "deploy": "astro build && touch dist/.nojekyll && gh-pages -d dist --dotfiles",
    "astro": "astro"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.2",
    "astro": "^6.1.5",
    "gh-pages": "^6.3.0",
    "save-dev": "^0.0.1-security",
    "tailwindcss": "^4.2.2"
  }
}
